# healthcare-system/billing-service/src/main/resources/bootstrap.yml
spring:
  application:
    name: billing-service # Service name
  config:
    import: "configserver:${CONFIG_SERVER_URI:http://config-server:8888}"
  cloud:
    config:
      enabled: true
      import-check:
        enabled: true