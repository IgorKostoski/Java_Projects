spring:
  application:
    name: patient-service

  config:
    import: "configserver:${CONFIG_SERVER_URI:http://config-server:8888}"  # ✅ MUST be here

  cloud:
    config:
      enabled: true
      import-check:
        enabled: true


  eureka:
    client:
      registry-fetch-interval-seconds: 5
      initial-instance-info-replication-interval-seconds: 5

